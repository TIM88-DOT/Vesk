{
  "name": "vesk-qa",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Autonomous QA harness — Playwright UI journeys + API probes against the running Vesk app.",
  "scripts": {
    "smoke": "node scenarios/smoke.mjs",
    "install:browser": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  }
}
